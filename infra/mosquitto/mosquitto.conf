# Local prototype broker: anonymous access, no TLS. Add auth before exposing outside the local network.
listener 1883
allow_anonymous true
persistence true
persistence_location /mosquitto/data/
log_dest stdout
